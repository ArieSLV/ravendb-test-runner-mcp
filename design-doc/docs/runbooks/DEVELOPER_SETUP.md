# Developer Setup

## Purpose
Describe how a developer prepares a local machine for RavenDB Test Runner MCP Server.

## Minimum content expected from implementation
- install .NET 10 SDK baseline
- provide embedded RavenDB license through approved probe order
- choose artifact root
- start the local standalone application
- verify MCP Streamable HTTP endpoint and dashboard startup
